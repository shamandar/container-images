name: Docker Image CI

on:
  workflow_dispatch:
    branches: [ dev/* ]
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Build all Docker images
      run: make -j all
